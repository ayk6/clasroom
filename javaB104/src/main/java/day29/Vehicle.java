package day29;

public interface Vehicle {
    // class - interface -> implements
    // class - class -> extends
    // interface - interface -> extends
    // interface - class -> cannot be encapsulation
}
